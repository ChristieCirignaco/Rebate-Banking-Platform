// The illustration behind both admin auth screens — the sign-in panel and the idle lock
// screen. Shared so the two stay visually in step and the path lives in one place; swapping the
// asset is a single edit here rather than a hunt through components.
//
// Not a setting: this is chrome for the two screens only admins ever see, and adding it to the
// Branding settings would imply an operator is meant to tune it.
export const ADMIN_AUTH_ARTWORK = "/mobile-payment-illustration.jpg";

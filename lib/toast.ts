// Single import path for toasts. Project convention: use react-hot-toast for every
// alert/notification — never inline alert banners. Import `toast` from here.
export { default as toast } from "react-hot-toast";
